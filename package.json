{
  "name": "pisco-sample",
  "version": "0.1.0",
  "description": "My first recipe",
  "main": "bin/pisco.js",
  "bin": {
    "recipe-sample": "bin/pisco.js"
  },
  "scripts": {
    "deps": "npm install",
    "test": "istanbul cover --report lcovonly --dir reports node_modules/.bin/_mocha -- --recursive -R spec"
  },
  "keywords": [
    "piscosour-recipe"
  ],
  "license": "Apache Licence",
  "preferGlobal": true,
  "dependencies": {
    "piscosour": "^1.0.0"
  },
  "devDependencies": {
    "chai": "*",
    "mocha": "*",
    "xunit-file": "^1.0.0"
  },
  "engines": {
    "node": ">=4.0.0"
  }
}
